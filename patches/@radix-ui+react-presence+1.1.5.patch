diff --git a/node_modules/@radix-ui/react-presence/dist/index.js b/node_modules/@radix-ui/react-presence/dist/index.js
index 944abc2..5655448 100644
--- a/node_modules/@radix-ui/react-presence/dist/index.js
+++ b/node_modules/@radix-ui/react-presence/dist/index.js
@@ -62,6 +62,7 @@ var Presence = (props) => {
 Presence.displayName = "Presence";
 function usePresence(present) {
   const [node, setNode] = React2.useState();
+  const nodeRef = React2.useRef(null);
   const stylesRef = React2.useRef(null);
   const prevPresentRef = React2.useRef(present);
   const prevAnimationNameRef = React2.useRef("none");
@@ -146,6 +147,8 @@ function usePresence(present) {
   return {
     isPresent: ["mounted", "unmountSuspended"].includes(state),
     ref: React2.useCallback((node2) => {
+      if (nodeRef.current === node2) return;
+      nodeRef.current = node2;
       stylesRef.current = node2 ? getComputedStyle(node2) : null;
       setNode(node2);
     }, [])
diff --git a/node_modules/@radix-ui/react-presence/dist/index.mjs b/node_modules/@radix-ui/react-presence/dist/index.mjs
index 0efe02a..e11c4cc 100644
--- a/node_modules/@radix-ui/react-presence/dist/index.mjs
+++ b/node_modules/@radix-ui/react-presence/dist/index.mjs
@@ -15,6 +15,7 @@ function useStateMachine(initialState, machine) {
 }
 
 // src/presence.tsx
+// PATCHED: Fix infinite loop with React 19 ref cleanup functions
 var Presence = (props) => {
   const { present, children } = props;
   const presence = usePresence(present);
@@ -26,6 +27,7 @@ var Presence = (props) => {
 Presence.displayName = "Presence";
 function usePresence(present) {
   const [node, setNode] = React2.useState();
+  const nodeRef = React2.useRef(null);
   const stylesRef = React2.useRef(null);
   const prevPresentRef = React2.useRef(present);
   const prevAnimationNameRef = React2.useRef("none");
@@ -110,6 +112,8 @@ function usePresence(present) {
   return {
     isPresent: ["mounted", "unmountSuspended"].includes(state),
     ref: React2.useCallback((node2) => {
+      if (nodeRef.current === node2) return;
+      nodeRef.current = node2;
       stylesRef.current = node2 ? getComputedStyle(node2) : null;
       setNode(node2);
     }, [])
